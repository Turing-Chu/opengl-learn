# A Example OpenGL Program

CMakeLists.txt for CLion on Mac

if you build it with gcc/g++, you can 


```shell
gcc -o untitled main.cpp -L/System/Library/Frameworks -framework GLUT -framework OpenGL
```
